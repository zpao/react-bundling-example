{
  "name": "react-bundling-example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "dependencies": {
    "browserify": "^6.2.0",
    "react": "^0.12.0",
    "reactify": "^0.15.2"
  },
  "scripts": {
    "bundle": "NODE_ENV=development browserify -t reactify index.js -o bundle.js"
  }
}
